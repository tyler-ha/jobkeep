{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // Left empty here on purpose. A deployed environment (Phase 3, AWS RDS)
    // supplies the connection string via the ConnectionStrings__Postgres
    // environment variable. appsettings.Development.json fills it for local dev.
    "Postgres": ""
  }
}
